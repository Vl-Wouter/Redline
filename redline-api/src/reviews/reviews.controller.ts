import { Controller } from '@nestjs/common';

@Controller('reviews')
export class ReviewsController {
  /**
   * @todo Add Controller routes for reviews
   * @body the review controller still needs create, get and delete routes
   *
   */
}
